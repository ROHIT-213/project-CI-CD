{
  "name": "@project-ci-cd/profile-update",
  "version": "1.0.0",
  "description": "Profile update REST API",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "mkdir -p dist && cp -r src dist",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
